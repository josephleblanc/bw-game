[package]
name = "bw-demo"
version.workspace = true
rust-version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
bw-core.workspace = true
anyhow.workspace = true
bevy.workspace = true
serde.workspace = true
serde_json.workspace = true

[features]
# Allocation-counting pass build (ADR 0003): separate build/run so the
# counting allocator never perturbs timing measurements.
perf-alloc = []

[lints]
workspace = true
